# This app sits behind a login: it is software, not content.
#
# The SPA catch-all serves the same shell for any path that is not a real file,
# so crawling below the root yields an unbounded set of identical URLs. The
# root stays allowed as the sign-in entry point.
User-agent: *
Allow: /$
Disallow: /
